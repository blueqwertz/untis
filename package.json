{
	"name": "untis",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"start": "concurrently 'npm run start --prefix client' 'npm run start --prefix server'",
		"dev": "concurrently 'npm run start --prefix client' 'npm run dev --prefix server'",
		"build": "(cd client && npm install) && CI=false npm run build --prefix client"
	},
	"keywords": [
		"school",
		"timetable",
		"students",
		"teachers",
		"rooms"
	],
	"author": "blueqwertz",
	"license": "ISC",
	"dependencies": {
		"client": "^0.0.1",
		"concurrently": "8.0.1",
		"react-hot-toast": "^2.4.0"
	}
}
